import { returnEntryChunkData, generateHtmlPages, } from "./metaMapPluginLogic.js";
function metaMapPlugin(options) {
    let outDir;
    let base;
    let generatedEntryChunks;
    return {
        name: "vite-plugin-react-meta-map",
        configResolved(resolvedConfig) {
            // get the output directory from Vite config
            outDir = resolvedConfig.build.outDir;
            base = resolvedConfig.base;
        },
        generateBundle(_, bundle) {
            // track entry point assets generated by vite
            generatedEntryChunks = returnEntryChunkData(bundle);
        },
        async closeBundle() {
            const { pageTemplateFilePath, pageMetaMapFilePath } = options;
            await generateHtmlPages(outDir, base, pageTemplateFilePath, pageMetaMapFilePath, generatedEntryChunks);
        },
    };
}
export default metaMapPlugin;
//# sourceMappingURL=index.js.map